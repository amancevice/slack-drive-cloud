{
  "cloud": {
    "project": "${project}",
    "redirect": "${redirect}",
    "region": "${region}"
  },
  "slack": {
    "channel": "${channel}",
    "color": "${color}",
    "slash_command": "${slash_command}",
    "verification_token": "${verification_token}",
    "web_api_token": "${web_api_token}"
  }
}
